import React from 'react';
import { View } from 'react-native';

import { AuthRoutes } from './AuthRoutes';

export const Routes = () => {
    return (
        <View>
            <AuthRoutes />
        </View>
    )
}
